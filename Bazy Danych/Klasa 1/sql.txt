SELECT s.marka, s.model, k.imie_klienta, k.nazwisko_klienta FROM samochody s JOIN dane_wypozyczen d ON s.id_samochodu =  d.id_samochodu JOIN wypozyczenia w ON d.id_wypozyczenia = w.id_wypozyczenia JOIN klienci k ON w.id_klienta = k.id_klienta WHERE k.id_klienta BETWEEN 1 AND 3

SELECT s.marka, s.model, k.email_klienta, k.imie_klienta, k.nazwisko_klienta, w.data_wyp, w.data_zwr FROM samochody s JOIN dane_wypozyczen d ON s.id_samochodu =  d.id_samochodu JOIN wypozyczenia w ON d.id_wypozyczenia = w.id_wypozyczenia JOIN klienci k ON w.id_klienta = k.id_klienta WHERE k.id_klienta BETWEEN 1 AND 5 LIMIT 10

SELECT s.marka, s.model FROM samochody s JOIN dane_wypozyczen d ON s.id_samochodu =  d.id_samochodu JOIN wypozyczenia w ON d.id_wypozyczenia = w.id_wypozyczenia JOIN klienci k ON w.id_klienta = k.id_klienta  JOIN wypozyczenia ON k.id_klienta = w.id_klienta JOIN pracownicy p ON w.id_pracownika = p.id_pracownika WHERE p.imie_pracownika LIKE "A%" AND p.imie_pracownika NOT LIKE "%B"

SELECT COUNT(w.id_wypozyczenia) FROM samochody s JOIN dane_wypozyczen d ON s.id_samochodu =  d.id_samochodu JOIN wypozyczenia w ON d.id_wypozyczenia = w.id_wypozyczenia JOIN klienci k ON w.id_klienta = k.id_klienta  JOIN wypozyczenia ON k.id_klienta = w.id_klienta JOIN pracownicy p ON w.id_pracownika = p.id_pracownika WHERE p.id_pracownika = 2 OR k.id_klienta = 5

SELECT COUNT(wypozyczenia.id_wypozyczenia) FROM wypozyczenia WHERE wypozyczenia.id_pracownika = 2 
SELECT COUNT(wypozyczenia.id_wypozyczenia) FROM wypozyczenia WHERE wypozyczenia.id_klienta = 5

2 Dzień

SELECT DISTINCT w.data_wyp, p.imie_pracownika, p.nazwisko_pracownika FROM samochody s JOIN dane_wypozyczen d ON s.id_samochodu =  d.id_samochodu JOIN wypozyczenia w ON d.id_wypozyczenia = w.id_wypozyczenia JOIN klienci k ON w.id_klienta = k.id_klienta  JOIN wypozyczenia ON k.id_klienta = w.id_klienta JOIN pracownicy p ON w.id_pracownika = p.id_pracownika WHERE w.data_wyp LIKE "2022-07-%%" AND (s.marka = "bmw" OR s.marka = "mercedes")

Zeszyt Jedlikowski
SELECT f.title, f.description, f.rental_duration, f.rental_rate * f.rental_duration AS "Koszt_za_dzień" FROM film f
SELECT DISTINCT a.last_name FROM actor a
SELECT DISTINCT f.rating FROM film f
SELECT f.title, f.description, f.rating, f.length FROM film f WHERE f.length >= 180
SELECT * FROM customer c WHERE c.active = 0 XOR c.last_name LIKE "%M"
SELECT * FROM category c WHERE c.category_id > 4 AND c.name LIKE "C%" OR c.name LIKE "S%" OR c.name LIKE "T%"
SELECT * FROM category c WHERE LEFT(c.name, 1) IN ("C", "S", "T") AND c.category_id > 4 