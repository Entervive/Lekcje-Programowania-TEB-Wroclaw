1.
SELECT s.imie, s.miot
FROM swinki s
WHERE s.data_ur LIKE "%07%";

2.
SELECT DISTINCT s.data_ur, s.miot, r.rasa
FROM swinki s
JOIN rasy r
ON s.rasy_id = r.id
WHERE r.id = 1;

3.
SELECT s.imie, s.cena, s.opis
FROM swinki s
JOIN rasy r
ON s.rasy_id = r.id
WHERE r.id = 1;

4.
SELECT r.rasa FROM rasy r;